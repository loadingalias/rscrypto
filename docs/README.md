# Documentation

This directory holds the supporting material behind the v1 release story.

## What lives here

- [`tasks/aead.md`](tasks/aead.md): AEAD rollout and acceleration plan
- [`tasks/auth.md`](tasks/auth.md): auth, KDF, and signature rollout plan
- [`tasks/next_steps.md`](tasks/next_steps.md): benchmark and performance follow-up work

## Benchmarks

Benchmark results live in `benchmark_results/` at the repo root:

- `benchmark_results/OVERVIEW.md` — cross-platform competitive scoreboard
- `benchmark_results/$DATE/$OS/$ARCH/results.txt` — raw Criterion data per platform
