---
"rscrypto" = "patch"
---

Require ML-DSA secret-kernel evidence in the constant-time harness. Cover all
parameter sets with production timing adapters, retained linked-binary roots,
and a bounded portable Montgomery proof root. Preserve the existing sample
budgets and thresholds. Whole signing retains variable retries and remains
outside a strict constant-time claim.

Protect x86-64 scalar selections and secret sampler masks from compiler-created
branches. Preserve the standard arithmetic and sampling order.

Follow direct tail transfers when building linked-code call closures so optimized
evidence wrappers cannot hide the production kernel from assembly review.
