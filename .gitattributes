# Normalize line endings to LF in repository
* text=auto eol=lf

# Shell scripts MUST use LF (even on Windows Git Bash)
*.sh text eol=lf

# Windows scripts use CRLF
*.bat text eol=crlf
*.ps1 text eol=crlf

testdata/ascon/asconaead128.txt -diff
testdata/ascon/asconcxof128.txt -diff
