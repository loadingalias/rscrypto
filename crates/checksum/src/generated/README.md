# Generated tables (development)

`kernel_tables.rs` is generated by `scripts/gen/kernel_tables.py` from the
benchmark baselines in `crates/checksum/bench_baseline/`.

This output is a development aid for inspecting benchmark results and
prototyping dispatch table updates. The production dispatch tables live in
`crates/checksum/src/dispatch.rs`.
