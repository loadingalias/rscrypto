[package]
name = "rscrypto-fuzz-support"
version = "0.0.0"
publish = false
edition = "2024"
license = "MIT OR Apache-2.0"

[features]
# Helpers only; the consuming fuzz package selects an rscrypto AEAD primitive.
aead = []
traits_io = []

[dependencies]
rscrypto = { path = "../..", default-features = false, features = ["std"] }
